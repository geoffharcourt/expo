en:
  date:
    formats:
      custom:
        "%b %-d, %Y"
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %e, %Y"
      short:
        "%B %d"

  titles:
    application: Expo

  helpers:
    submit:
      airport:
        create: Create Airport

  airports:
    create:
      success: New airport created!
    index:
      add_airport: Add An Airport
      title: Airports
    new:
      title: New Airport
